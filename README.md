# space - manage spaces through a CLI

`space` is a CLI tool that launches apps specified in a collection called a
space.

## Installing

```shell
cargo install --git ssh://git@github.com/calejvaldez/space.git
```
